<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/form.css"> 
    <title>Edit Movie</title>
</head>
<body>
    <div class="form-container">
        <h1 class="form-title">Edit Movie</h1>
        <form action="/<%= movie._id %>" method="POST" enctype="multipart/form-data" class="movie-form">
            <label for="title" class="form-label">Title</label>
            <input type="text" name="title" class="form-input" value="<%= movie.title %>" required>

            <label for="description" class="form-label">Description</label>
            <textarea name="description" class="form-textarea" required><%= movie.description %></textarea>

            <label for="releaseDate" class="form-label">Release Date</label>
            <input type="date" name="releaseDate" class="form-input" value="<%= movie.releaseDate.toISOString().substring(0, 10) %>" required>

            <label for="genre" class="form-label">Genre</label>
            <input type="text" name="genre" class="form-input" value="<%= movie.genre %>" required>

            <label for="rating" class="form-label">Rating</label>
            <input type="number" name="rating" class="form-input" min="1" max="10" value="<%= movie.rating %>" required>

            <label for="poster" class="form-label">Poster</label>
            <input type="file" name="poster" class="form-file">
            <img src="<%= movie.poster %>" alt="<%= movie.title %> Poster" class="poster-image" width="100">

            <button type="submit" class="form-button">Update Movie</button>
        </form>
    </div>
</body>
</html>
